BINARY := {{.ProjectName}}

.PHONY: build run generate lint clean

build:
	CGO_ENABLED=0 go build -o $(BINARY) ./cmd/

run:
	go run ./cmd/

generate:
	go generate ./internal/api/...

lint:
	golangci-lint run ./...

clean:
	rm -f $(BINARY)
